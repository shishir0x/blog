import type { FooterConfig } from "../types/config";

export const footerConfig: FooterConfig = {
  enable: false, // Whether to enable Footer HTML injection feature
};

// Directly edit config/FooterConfig.html file to add custom content like ICP registration numbers
